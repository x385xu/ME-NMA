`compute_AIC_multiarm.R`: function to compute the AIC values of random-effects model, fixed-effect model and the multiplicative-effect model

`get_index_nmadb.R`: get indices of NMA dataset of which reported required measures, and indicate whether it contains multi-arm studies

`nmadb_aic_multiarm.R`: File that computes the AIC values for additive random-effects model, fixed effect model and multiplicative effect model and plot AIC differences

`nmadb_analysis_01(02, 03, 04).R`: Case studies
